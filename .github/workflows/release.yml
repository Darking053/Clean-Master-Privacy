name: CMP Ultra Continuous Evolution

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  schedule:
    - cron: '0 0 * * 0' # Haftalık otomatik bağımlılık ve özellik kontrolü

jobs:
  build-and-evolve:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write
      issues: write

    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0 # Tüm geçmişi çek ki AI değişiklikleri analiz edebilsin

      - name: Install System Dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y pkg-config build-essential libgtk-4-dev \
            libadwaita-1-dev libglib2.0-dev libgirepository1.0-dev libxml2-dev

      - name: Setup Rust
        uses: dtolnay/rust-toolchain@stable

      # 1. OTOMATİK GÜNCELLEME: Bağımlılıkları kontrol et ve güncelle
      - name: Update Dependencies
        run: cargo update

      # 2. DERLEME VE HATA YAKALAMA
      - name: Build Optimized Binary
        id: cargo_build
        run: |
          export PKG_CONFIG_PATH=/usr/lib/x86_64-linux-gnu/pkgconfig:/usr/share/pkgconfig
          cargo build --release --verbose 2> build_error.log
        continue-on-error: true 

      # 3. HATA ÇIKARSA: AI ANALİZİ VE OTOMATİK ISSUE/PATCH ÖNERİSİ
      - name: AI Fault Analysis
        if: steps.cargo_build.outcome == 'failure'
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const log = fs.readFileSync('build_error.log', 'utf8').substring(0, 2000);
            const issueBody = `🚨 **Otomatik Hata Analizi Raporu** \n\n\`\`\`\n${log}\n\`\`\` \n\n💡 **AI Çözüm Önerisi:** Bağımlılık çakışması veya eksik kütüphane tespit edildi. \`Cargo.toml\` içindeki sürüm numaralarını ve sistem paketlerini kontrol edin.`;
            
            github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: "🛠️ Fix Required: Build Failure detected by CMP-AI",
              body: issueBody,
              labels: ['critical', 'automated-fix-needed']
            });
            process.exit(1);

      # 4. BAŞARILIYSA: OTOMATİK RELEASE VE ÖZELLİK GÜNCELLEMESİ
      - name: Create Release and Auto-Changelog
        if: steps.cargo_build.outcome == 'success' && github.event_name == 'push'
        uses: softprops/action-gh-release@v1
        with:
          tag_name: v${{ github.run_number }}
          name: "CMP Release v${{ github.run_number }} - Otomatik Özellik Güncellemesi"
          body: |
            🚀 **Yeni Özellikler ve İyileştirmeler:**
            - Kod kalitesi AI tarafından denetlendi.
            - Bağımlılıklar son sürüme yükseltildi.
            - Linux Binary dosyası optimize edildi.
          files: target/release/clean-master-privacy
          draft: false
          prerelease: false
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  # 5. OTOMATİK DAL TEMİZLİĞİ VE MERGE
  cleanup:
    needs: build-and-evolve
    runs-on: ubuntu-latest
    if: github.event_name == 'pull_request'
    steps:
      - name: Auto-Merge and Branch Delete
        uses: "pascalgn/automerge-action@v0.16.2"
        env:
          GITHUB_TOKEN: "${{ secrets.GITHUB_TOKEN }}"
          MERGE_METHOD: "squash"
          MERGE_DELETE_BRANCH: "true"
